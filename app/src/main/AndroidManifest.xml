<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ysq.example.album">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.PersonalActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.PersonalActivity" />
        <activity
            android:name=".activity.BrowseActivity"
            android:label="@string/picture_browse"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.PersonalActivity" />
        <activity
            android:name=".activity.PreviewActivity"
            android:label=""
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.PreviewTheme" />
    </application>

</manifest>